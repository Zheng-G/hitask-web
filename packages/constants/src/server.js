/**
 * @module constants/server
 */

/**
 * Codes of handled server responses
 * @enum {Number}
 */
export const ResponseStatus = {
	NOT_AUTH: 401,
	FORBID: 403,
};

export const ResponseCode = {
	TIMEOUT: 'ECONNABORTED',
};
