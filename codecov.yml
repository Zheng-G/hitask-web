coverage:
  precision: 2
  round: down
  range: "28...100"

  status:
    project:
      default:
        target: auto
        threshold: 1%
        branches: null

    patch:
      default:
        target: 0%
        branches: null

    changes:
      default:
        branches: null

  ignore: null

comment:
  layout: "header, diff, changes, sunburst, uncovered, tree"
  branches: null
  behavior: default
